package com.codeup.CoopCredit.infrastructure.adapters.in.web.mapper;

import org.mapstruct.Mapper;

@Mapper(componentModel = "spring")
public interface AuthWebMapper {

    // Additional mappings for Auth DTOs if needed
}
